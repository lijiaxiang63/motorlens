/// <reference types="vite/client" />

/** Injected by vite.config.ts / vitest.config.ts's `define` from package.json
 * — single source of truth for the version embedded in exported reports and
 * shown in Settings. See src/config.ts's `APP_VERSION`. */
declare const __APP_VERSION__: string
